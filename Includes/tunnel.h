#ifndef TUNNEL_H
#define TUNNEL_H

#include "base_vertex.h"

static const vertex tunnel_vertex_list[] =
{
	
};

#define tunnel_vertex_list_count (sizeof(tunnel_vertex_list)/sizeof(tunnel_vertex_list[0]))

#endif